/* Hero intro timing. Everything (headline, body, download CTA) reveals
 * together on landing, and the desktop mock below rises in the same beat (it
 * imports CTA_DELAY). */
export const HERO_DELAY = 120;
export const HERO_DUR_MS = 1000;
export const CTA_DELAY = HERO_DELAY;
